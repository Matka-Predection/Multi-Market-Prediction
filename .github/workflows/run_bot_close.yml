name: Run Live Kalyan Close Tracker Loop

on:
  schedule:
    # Wakes up automatically Monday to Friday at 7:30 PM IST (02:00 PM UTC)
    - cron: '0 14 * * 1-5'
  workflow_dispatch: 

jobs:
  run-script:
    runs-on: ubuntu-latest
    env:
      TELEGRAM_TOKEN: ${{ secrets.MY_SECRET_BOT_KEY }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
    steps:
      - uses: actions/checkout@v5
      - run: pip3 install requests beautifulsoup4 pytz
      - run: python3 simple_predict.py
      - name: Save State
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@://github.com"
          git add last_msg_id.txt || true
          git commit -m "Update state [skip ci]" || true
          git push || true
